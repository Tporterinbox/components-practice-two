function PersonCard({ name, age, occupation }) {
    return (
      <div>
        <h2>{name}</h2>
        <p>{age}</p>
        <p>{occupation}</p>
      </div>
    );  
  }
  
  export default PersonCard;
  